<Window x:Class="CATERINGMANAGEMENT.View.Windows.AddProfile"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CATERINGMANAGEMENT.ViewModels" d:DataContext="{d:DesignInstance Type=vm:ProfileViewModel}"
        mc:Ignorable="d"
        Title="Add New Profile" Height="450" Width="500"
        WindowStartupLocation="CenterScreen">


    <Grid Background="#F4FAFF" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Add Your Profile"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#2E4A62"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Name -->
        <StackPanel Grid.Row="1" Margin="0,5">
            <TextBlock Text="Name" Margin="0,0,0,5" Foreground="#3A536B"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30"/>
        </StackPanel>

        <!-- Address -->
        <StackPanel Grid.Row="3" Margin="0,5">
            <TextBlock Text="Address" Margin="0,10,0,5" Foreground="#3A536B"/>
            <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30"/>
        </StackPanel>

        <!-- Contact Number -->
        <StackPanel Grid.Row="4" Margin="0,5">
            <TextBlock Text="Contact Number" Margin="0,10,0,5" Foreground="#3A536B"/>
            <TextBox Text="{Binding ContactNumber, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30"/>
        </StackPanel>


        <!-- Submit Button -->
        <Button Grid.Row="6"
                Content="Save Profile"
                Width="140"
                Height="40"
                HorizontalAlignment="Right"
                Margin="0,20,0,0"
                Background="#3A87C7"
                Foreground="White"
                FontWeight="SemiBold"
                Command="{Binding SaveCommand}"/>
    </Grid>
</Window>
