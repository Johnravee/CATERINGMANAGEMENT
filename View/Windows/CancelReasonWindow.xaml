<Window x:Class="CATERINGMANAGEMENT.View.Windows.CancelReasonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cancellation Reason" Height="240" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Please provide a brief reason for cancelling this reservation (not stored):"
                   TextWrapping="Wrap"/>

        <TextBox x:Name="ReasonBox" Grid.Row="1" Margin="0,8,0,8" AcceptsReturn="True" TextWrapping="Wrap"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2">
            <Button Width="90" Margin="6" Click="Cancel_Click">Cancel</Button>
            <Button Width="90" Margin="6" Click="Ok_Click">OK</Button>
        </StackPanel>
    </Grid>
</Window>