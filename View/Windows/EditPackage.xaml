<Window x:Class="CATERINGMANAGEMENT.View.Windows.EditPackage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Height="280"
        Width="420"
        Background="#F9FAFB"
        FontFamily="Segoe UI">

    <Border CornerRadius="12"
            Background="White"
            Padding="20"
            Margin="10"
            Effect="{DynamicResource DropShadow}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Text="Edit Package"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#2D2D2D"
                       Margin="0,0,0,20"/>

            <!-- Name Field -->
            <StackPanel Grid.Row="1" Margin="0,0,0,15">
                <TextBlock Text="Name" Foreground="#555" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                         CharacterCasing="Upper"
                         Padding="8"
                         FontSize="14"
                         Background="#F3F4F6"
                         BorderThickness="1"
                         BorderBrush="#D1D5DB"/>
            </StackPanel>

            

            <!-- Buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Cancel"
                        Width="90"
                        Height="32"
                        Margin="0,0,10,0"
                        Command="{Binding CancelCommand}"
                        Background="#E5E7EB"
                        Foreground="#333"
                        BorderBrush="#D1D5DB"
                        BorderThickness="1"/>
                <Button Content="Save"
                        Width="90"
                        Height="32"
                        Command="{Binding SaveCommand}"
                        Background="#4F46E5"
                        Foreground="White"
                        BorderThickness="0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
